{
  "name": "neuralnector",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mkdir -p ../backend/static && cp -r dist/* ../backend/static/ && cd ../backend && python3 -m ensurepip --upgrade && python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt",
    "start": "cd backend && python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT"
  }
}

